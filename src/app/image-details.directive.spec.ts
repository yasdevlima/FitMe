const { ImageDetailsDirective } = require('./image-details.directive');

describe('ImageDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
